### Build artifacts
debug/
target/

# Local Postgres test cluster (if ever created natively)
.pgdata/

# Content dir is the source of truth, bind-mounted at runtime — not built in.
miku/

# VCS / editor
.git/
.idea/

# rustfmt backups
**/*.rs.bk

# Cargo.lock is intentionally NOT ignored: the Docker build uses
# `cargo build --locked` for reproducible images.
